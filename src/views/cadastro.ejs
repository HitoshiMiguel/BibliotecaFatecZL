<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastro - Fatec</title>
  <link rel="stylesheet" href="/CSS/cadastro.css">
</head>
<body>

  <!-- Header com logos -->
  <header>
    <div class="govsp-header">
      <div class="logo-governo">
        <img src="/imagens/logo-governo.png" alt="Logo Governo SP">
      </div>
      <div class="icones">
        <a href="#"><img src="/imagens/i-facebook.png" alt="Facebook"></a>
        <a href="#"><img src="/imagens/i-linkedin.png" alt="LinkedIn"></a>
        <a href="#"><img src="/imagens/i-youtube.png" alt="YouTube"></a>
        <a href="#"><img src="/imagens/i-instagram.png" alt="Instagram"></a>
        <a href="#"><img src="/imagens/i-twitter.png" alt="Twitter"></a>
        <span class="govsp">/governosp</span>
      </div>
    </div>

    <div class="CPS-header">
      <div class="CPS-container">
        <div class="logos">
          <img src="/imagens/logo-fatec-cps.png" alt="Logo Fatec-CPS">
        </div>
      </div>
    </div>
  </header>

  <!-- Título -->
  <section class="title-section">
    <h1>Crie seu cadastro</h1>
  </section>

  <!-- Mensagens -->
  <main class="form-container">
    <% if (success) { %>
      <p style="color: green; margin-bottom: 1rem;"><%= success %></p>
    <% } %>
    <% if (errors && errors.global) { %>
      <p style="color: red; margin-bottom: 1rem;"><%= errors.global %></p>
    <% } %>

    <!-- Formulário -->
    <form action="/cadastro" method="POST" class="cadastro-form" novalidate>
      <div class="row">
        <div class="field">
          <label for="nome">Nome *</label>
          <input
            type="text"
            id="nome"
            name="nome"
            value="<%= (old && old.nome) || '' %>"
            required
          >
          <% if (errors && errors.nome) { %>
            <small style="color: red;"><%= errors.nome %></small>
          <% } %>
        </div>
      </div>

      <div class="field">
        <label for="ra">RA *</label>
        <input
          type="text"
          id="ra"
          name="ra"
          required
          inputmode="numeric"
          pattern="^\d{13}$"
          minlength="13"
          maxlength="13"
          value="<%= (old && old.ra) ? old.ra : '' %>"
        />
        <% if (errors && errors.ra) { %>
          <div class="erro"><%= errors.ra %></div>
        <% } %>

      <div class="field">
        <label for="email">Email *</label>
        <input
          type="email"
          id="email"
          name="email"
          value="<%= (old && old.email) || '' %>"
          required
        >
        <% if (errors && errors.email) { %>
          <small style="color: red;"><%= errors.email %></small>
        <% } %>
      </div>

      <div class="field">
        <label for="senha">Senha *</label>
        <input type="password" id="senha" name="senha" required>
        <% if (errors && errors.senha) { %>
          <small style="color: red;"><%= errors.senha %></small>
        <% } %>
      </div>

      <div class="field">
        <label for="confirmarSenha">Confirmar Senha *</label>
        <input type="password" id="confirmarSenha" name="confirmarSenha" required>
        <% if (errors && errors.confirmarSenha) { %>
          <small style="color: red;"><%= errors.confirmarSenha %></small>
        <% } %>
      </div>

      <button type="submit" class="btn-submit">Cadastrar-se</button>

      <p class="terms">
        Ao preencher o formulário acima você concorda com os nossos
        <a href="#">Termos de uso</a> e nossa
        <a href="#">Política de Privacidade</a>.
      </p>
    </form>
  </main>

</body>
</html>
